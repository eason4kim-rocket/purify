# Purify Search — 三层信任栈 · 锁定版作战计划

| 项 | 值 |
|---|---|
| 版本 | v1.0（锁定版） |
| 日期 | 2026-08-10 |
| 状态 | **LOCKED** — 方向定案，不再变来变去。只允许在本框架内增删任务卡，不允许推翻三层结构。 |
| 定位 | 这是**北极星**：回答「我们在造什么、按什么顺序、为什么不会变」。 |
| 与其他文档的关系 | 本文档是 WHY + WHAT + 顺序；`MASTERPLAN.md` / `SCRAPE-PLAN.md` / `SEARCH.md` 是每层的**任务卡（HOW）**。冲突时以本文档的**三层结构和非目标**为准，任务卡在其内部执行。 |
| 一句话战略 | 做 agent 时代的 search API，但赢法不是「更便宜的 Firecrawl / 更强的 Exa」，而是**唯一一条「信任贯穿抓取→检索→验证」三层的栈**。 |

---

## 0. 为什么这份文档锁定，以及被否决的岔路

研究了几个月，反复横跳，是因为一直在两个问题间摇摆：「卷底层供给，还是做上层判断？」「以小博大到底靠什么？」现在收敛，结论锁死：

- **两个都做，垂直整合。** 供给（抓取/检索）是上牌桌的门票和现金流；判断（验证）是别人抄不走的钉子。不是二选一。
- **以小博大不靠「单层最强」，靠「三层焊接」。** 单层都拼不过：重排器是开源大路货，抓取的蛮力是资本战。但**「信任从抓取一路贯穿到验证」这条完整的栈，没有一家有**——这才是小团队能守、大厂结构上做不全的东西。

**已经明确否决、不要再走回去的岔路（写下来防止再漂移）：**

1. ❌ 自建搜索索引（Exa/Brave 的资本战场，H200 集群 + 300 亿页爬虫，烧不起）。
2. ❌ 烧推理算力做 deep-research 循环（大玩家的战场，`test-time compute` 军备赛）。
3. ❌ 正面做「通用可信搜索」硬刚 Parallel + Google（Parallel $20 亿、Parag Agrawal、已被 Google Gemini Enterprise 集成为原生 grounding；那条车道有人了）。
4. ❌ 把全部身家押在第三层一个前沿赌注上（上限最高但需求未证；要有能赚钱的底座托着）。

**我们要打的，是别人结构性做不了的两个洞**（见 §5）：**实体归因（EAV）** 和 **真实证据独立性（N_eff v1）**。连 $20 亿的 Parallel 都没做。

> **EN —** Locked thesis: build an agent-era search API, but win not as "cheaper Firecrawl / stronger Exa" — win as the only stack where **trust flows through all three layers (fetch → retrieve → verify)**. The moat is the *fusion*, not any single layer's peak. Explicitly refuse: self-built index, reasoning-loop compute war, head-on "verifiable search" against Parallel+Google, and betting everything on a single frontier feature.

---

## 1. 三层信任栈 · 总览

一条流水线：**一个 agent 的问题从下面进去，一个可信的答案从上面出来。**

```text
  ┌─────────────────────────────────────────────────────────┐
  │  第三层 · 验证 (Verify)          "拿到的东西能不能信"        │
  │  证据锚定 / 复验 / 收据 / 共识 / [新] EAV 实体归因 / N_eff v1 │  ← 已建 ~70%，最防守
  ├─────────────────────────────────────────────────────────┤
  │  第二层 · 检索/重排 (Retrieve)   "给问题，找该读哪些、排好序"  │
  │  provider 聚合 / [新] 0.6B 重排器 / [新] 按信任信号排序        │  ← 进行中 (codex/search-api-v1)
  ├─────────────────────────────────────────────────────────┤
  │  第一层 · 抓取 (Fetch+Extract)   "把一个网页搞到手、弄干净"   │
  │  多引擎升级 / 清洗 / 编译式提取 / [新] 数据平面提取           │  ← 骨架已有，缺"苹果"那条路
  └─────────────────────────────────────────────────────────┘
        每层的输出都保住「出处」，让上一层的信任可计算。
```

**护城河一句话：** 抓取时**保住出处** → 排序时**按独立性和实体匹配排** → 出货前**抓住「引对了页、说错了对象」**。三层焊成一条不断线的信任链。

**没有一家能抄这条贯穿：** Firecrawl 有第 1 层没第 3 层；Parallel 有第 2 层 + 半个第 3 层（引用+置信度）但**没有 N_eff / EAV，也不开放第 1 层的提取智能**；Exa 有第 2 层索引没有真正的第 3 层判断。

---

## 2. 现状盘点 · 每层已建什么（诚实的好消息）

| 层 | 干什么 | **已建（真实代码/提交）** | **新赌注（本计划新增）** |
|---|---|---|---|
| **L1 抓取** | URL → 干净结构化内容 | `engine/` 多引擎竞速升级（http→rod→rod-stealth）+ 自适应页池；`cleaner/` 9 文件清洗（readability/pruning/markdown/citations/selector）；`compiler/` 编译式提取器 + 模板簇 + 漂移 + 自愈（P2/P3 全 ✅）；`SCRAPE-PLAN.md` 质量门控计划 | **数据平面提取**（网络拦截 + 值重叠归因）——目前完全没有 |
| **L2 检索/重排** | query → 排序好的相关来源 | `search/` + `SEARCH.md` M0–M4：provider 中立契约、`/api/v1/search`、enrichment（进行中，`codex/search-api-v1`） | **0.6B 重排器 + 按信任信号排序**——目前是 provider 转卖，无重排、无信任排序 |
| **L3 验证** | 内容能不能信 | `evidence/` 值→锚点对齐；`snapshot/` 内容寻址快照；`receipts/` Ed25519 签名收据；`verify/` 三态复验；`ledger/` SQLite 持久层；`consensus/` 多源合并 + **N_eff v0**（`8a30f74`） | **① EAV 实体归因校验** + **② N_eff v0→v1（出处血缘独立）**——EAV 完全没有；N_eff 只到 v0（同域去重级） |

**关键认知：最难、最防守的第三层，你已经领先。** 新工作其实只有精确的三块：L1 的数据平面、L2 的重排器+信任排序、L3 的 EAV + N_eff v1。

---

## 3. 第一层 · 抓取（Fetch + Extract）

### 3.1 定义与边界
给一个 URL，拿到它**干净的、带出处的**结构化内容。只关心「一个页面/一个源」。

**「最好」要限定范围（锁定）：**
- ✅ **聪明那半**做到最好：自动发现数据源、配方复用、干净带类型输出、字段级出处。
- ⚖️ **蛮力那半**够用即可：过 Cloudflare/DataDome、代理池规模是烧钱的仗，**不与 Firecrawl fire-engine 正面拼资本**，`SCRAPE-PLAN.md` 的质量门控 + 现有 stealth 引擎够用。

### 3.2 已建（复用，不重写）
- `engine/`：http → rod → rod-stealth 梯次升级，自适应页池。
- `cleaner/`：readability / pruning / markdown / citations / selector / tokens。
- `compiler/`：LLM 引导编译出确定性提取器 IR，按模板簇缓存，漂移检测，自愈晋级（这是「表现平面」的 SNAP/苹果之外的另一条已建路径，保留作 fallback）。
- `SCRAPE-PLAN.md`：质量门控引擎升级、25 站验收矩阵、`quality{score,status,fetch_attempts}` 契约。**先按它把「抓得到、不空正文假成功」做扎实**，这是数据平面的地基。

### 3.3 新赌注 · 数据平面提取（网络拦截 + 值重叠归因）

**核心洞察：** 现代 70%+ 网页是 JS app 从后端 JSON API 拉数据再渲染成 HTML。别人（Firecrawl/Jina/我们的清洗管线）在「把干净 JSON 渲染成 HTML，再花 AI 反解回结构化」。**跳过 HTML，直接接管页面自己的数据 API。**

**算法（五步，落地到新包 `dataplane/`，复用 `engine/` 的 headless Chrome 作学习工具）：**

1. **首访 · CDP 网络拦截**：headless 加载，录下每个 XHR/fetch/GraphQL 的**响应 + 产生它的请求**（URL、method、headers、params、body）。
2. **内容-API 归因（不用 LLM）**：把页面可见的字符串/数字取出，对每个 JSON 响应算**值重叠度**（可见内容作为「值」出现的比例）；最高者即内容 API。纯集合求交，`simhash/` 可复用做模糊匹配。
3. **请求泛化（跨页差分）**：diff 同模板两页的中标请求，分出变量（slug/id/页码）与固定部分 → **参数化配方**。
4. **配方缓存**：按域+模板指纹缓存（存 `ledger/`，复用 `compiler/` 的模板簇/漂移基建）。后续同模板请求**跳过浏览器，纯 HTTP 直打 JSON API**。
5. **自愈**：直调失败（API 变更/token 过期）→ 退回浏览器重学配方（复用 `compiler/` 自愈闭环的模式）。

**为什么值得做（对客户的四个兑现）：**
- **更准**：网站自己的带类型 JSON，零提取误差、零幻觉。
- **更省 token**：结构化数据，从不碰 HTML，boilerplate 不存在。
- **内容更多**：API 常返回 UI 隐藏字段（原价、库存、内部 ID、分页游标）——**比人眼看到的还多**，HTML 抓取永远做不到。
- **更快**：学会后跳过浏览器，纯 HTTP，快 ~100×、成本趋近零。

**边界/fallback（锁定）：** 静态/服务端渲染页无独立数据 API → 退回 `compiler/`（编译提取）或 `cleaner/`（清洗）。两套是**叠的**，不是二选一。签名/鉴权 API 重放不了 → 浏览器留在环里，优雅降级。

**验收：**
- 20 个真实 SPA（电商/社媒/SaaS 列表）矩阵：≥ 80% 能自动定位到内容 API。
- 「数据平面拿到的字段数」相对「渲染页可见字段数」的**增量 > 0**（这是苹果对诺基亚的那块屏，是本赌注唯一的成败数字）。
- 学会配方后同模板第二次请求 P95 延迟 < 300ms（跳过浏览器）。

**诚实风险：** 这是**已知技术的优秀执行，不是发明**（ScrapEly/Apify 有手工/逐站版）。护城河是「通用自动化 + 配方库复利 + 带出处」，不是点子本身。ToS 灰度比抓公开 HTML 更灰，需在产品条款里标注。

---

## 4. 第二层 · 检索/重排（Retrieve + Rerank）

### 4.1 定义与边界
给一个**问题**（不是 URL），找出哪些页面值得读、按相关性排序。**不自建索引**（锁定）。

### 4.2 已建（复用）
- `SEARCH.md` M0–M4：`POST /api/v1/search`，provider 中立契约，`include_content` 用 `scraper/`+`cleaner/` enrich，HTTP + MCP 共用一个 service。**先按它把 MVP 打通**（provider 聚合 = 上牌桌）。

### 4.3 新赌注 · 0.6B 重排器 + 按信任信号排序

**现状问题：** SEARCH.md 是「转卖 provider + 可选 enrich」= 聚合档，**无差异化**。

**2026 SOTA 已证明（`Rerank Before You Reason`, ACL 2026）：** 深研循环的成本瓶颈是**输入 token**；一个 **0.6B cross-encoder 重排器**（成本 0.32×）保住绝大部分准确率增益，**「先重排再推理」比增加推理算力更省更准**。这直接利于小团队：**牛的小重排器 + 干净输入 > 烧推理算力的大团队。**

**两步（落地到 `search/rerank/`）：**

1. **基础重排（入场券）**：在 provider 返回的候选集上，跑一个开源 0.6B cross-encoder（如 qwen3-reranker 级），按相关性重排。这是 table stakes，不是护城河。
2. **信任信号排序（护城河）**：排序分不只按相关性，而是 **relevance × 独立性(N_eff) × 实体匹配(EAV)**——把**第三层的信号喂回重排器**。
   - 「10 个转载同一条 AP 通稿」的候选被 N_eff 折叠、降权。
   - 「引对了页但说的是错实体」的候选被 EAV 标记、降权。
   - **这个重排器别人造不出来，因为他们没有第三层。** 这是 L2 从大路货变成「我们的」的唯一地方。

**未决问题的定案（锁定）：** 候选从哪来？→ **起步用 provider 聚合（Brave/Tavily/等适配器），绝不自建索引。** 差异化全在「重排 + 信任排序」，不在「索引覆盖」。

**验收：**
- 重排把候选集的 NDCG@5 相对 provider 原序显著提升（对标论文 +100% 量级为上限参照）。
- 开启信任排序后，结果里的**独立来源占比**上升、**镜像转载占比**下降（用 `consensus/` 的 N_eff 度量）。
- 单次 `/search` 重排增量成本 < provider 调用成本（重排必须便宜，否则失去「以小博大」的意义）。

**诚实风险：** 基础重排是开源大路货，**不融合 L3 信号就没有护城河**。候选质量有上限——重排救不了 provider 没返回的页（这是不建索引的代价，接受）。

---

## 5. 第三层 · 验证（Verify / Judgment）——你最强、最领先的一层

### 5.1 定义
拿到的内容：**独立吗？是关于你问的那个东西吗？能溯源吗？** 输出带信任判断 + 出处的结果。

### 5.2 已建（这是你的领先优势，列清楚）
- `evidence/`：值→锚点对齐（quote + selector + 偏移 + 快照哈希）；`unlocated` 率天然是免费的幻觉度量。
- `snapshot/`：内容寻址快照库（CAS），支持事后可回放复验。
- `receipts/`：Ed25519 签名的可携带收据 + 公开验证端点。
- `verify/`：三态复验服务（supported / refuted / unlocatable）。
- `ledger/`：SQLite 统一持久层（migrations、窄事务、outbox）。
- `consensus/`：多源合并 + **N_eff v0**（`8a30f74`、`e0e918c`）——独立性计数的雏形。

**对手对比：** Parallel Basis 做到「字段级引用 + 摘录 + 校准置信度」，但**证据止于 URL+文字，不锚定 DOM、不存快照、无法事后复验**。你的 `evidence/`+`snapshot/`+`receipts/` 已经比它深。

### 5.3 新赌注 ① · EAV 实体归因校验（抓「欺骗性接地」）

**问题（`Deceptive Grounding` 论文实测 8–87%，新鲜事实更糟 13.6%）：** 系统如实转述真实文档里的真实数据，却安在**错误的实体**上（问 A，答的是 B 的真实数据 + 真实引用）。**对幻觉检测、忠实度、引用核查全部隐形**——因为每条声明都来自真实、被正确引用的文档，只是关于错的对象。**Parallel 的 Basis 结构上抓不到它。**

**算法（落点定案：新包 `verify/eav/`，任务卡见 `MASTERPLAN.md` §15 E-1…E-6；参照论文 97% 精度 / 98.7% 召回 / 0% 误报）：**
1. 找到支撑每条声明的文档（复用 `evidence/` 的锚点对齐）。
2. 抽出该文档的**主实体**。
3. 文档实体 ≠ 查询实体 → 报警「right-source-wrong-entity」。

**难点（诚实）：** 论文的 97% 是**窄的医疗场景（药名）**。推广到开放网络的任意实体类型（公司/人/产品/事件/地点）是**更硬的研究问题**——需要一个鲁棒的实体抽取 + 实体对齐层。这是本层的核心攻坚。

**验收：** 自建一个跨域「right-source-wrong-entity」标注集（≥ 数百条），EAV 精度 > 90%、召回 > 90%、干净样本误报 < 2%。

### 5.4 新赌注 ② · N_eff v0 → v1（出处血缘独立）

**现状 v0：** 同域去重级（「不同 URL/不同域 = 独立」）——会被「10 个域转载同一条通稿」骗（真实 N_eff=1）。

**v1 目标：** 从「域多样性」升级到「**谁抄了谁**」的出处血缘独立：
- 用 `simhash/`（已有全家：Fingerprint/Distance/Similar/FingerprintDOM）做近重复检测，把镜像/转载折叠成一个独立单元。
- 引入引用/时间先后/措辞血缘，估计真实独立来源数 N_eff。
- 喂给 L2 重排（§4.3）和 L3 置信度。

**验收：** 对「已知一源多镜像」的构造集，N_eff v1 正确折叠到接近 1；对真正多源独立事件，N_eff 接近来源数。

**诚实风险（整个 L3）：** 这是**前沿赌注**——上限最高、也最不确定。「空位」一半因为难，一半因为**需求未被大规模证明**（Parallel 靠「便宜+带引用」就赢单，说明市场现在觉得「够用」）。**打法：让 EAV/N_eff 当别人抄不走的差异化，焊在能赚钱的 L1+L2 底座上，不单独 all-in。**

---

## 6. 建造顺序（按此执行，依赖感知）

顺序的逻辑：**先把已在进行的 L2 打通（上牌桌）→ 再做最空最像你的 L3 差异化（证明护城河）→ 用 L3 信号点亮 L2 重排（焊接）→ 最后啃 L1 最大的新工程（数据平面）。**

| 步 | 做什么 | 层 | 为什么这个顺序 | 依赖 |
|---|---|---|---|---|
| **1** | 打通 `SEARCH.md` M0–M4 的 `/search` MVP（provider 聚合 + enrich） | L2 | 已在进行（当前分支），先上牌桌、有现金流 | 无 |
| **2** | **EAV 原型** | L3 | 最空、最像你、Parallel 结构做不了；有 97% 精度算法参照；痛点明确（新事实 13.6% 出错） | `evidence/` |
| **3** | **N_eff v0→v1**（出处血缘独立） | L3 | 建在你已有的 `consensus/` + `simhash/` 上，投入产出高 | `consensus/` `simhash/` |
| **4** | **基础重排器 + 信任信号排序**（把 EAV/N_eff 喂回排序） | L2 | 焊接的关键一步：让 L2 从大路货变成不可复制 | 步 2、3 |
| **5** | **数据平面提取**（网络拦截 + 值重叠归因） | L1 | 最大的新供给工程；在 L2/L3 差异化已证明后再重投入 | `engine/` `ledger/` `compiler/` |

> 你对 L1 数据平面有执念——它已获批（步 5），但**故意排在后面**：因为它是「已知技术的优秀执行」，价值在长期复利；而 EAV/N_eff 是「连前沿都没填的洞」，先做它们能最快证明「这条栈别人抄不了」。**先证明护城河，再加厚供给。**

**每一步的提交纪律**（沿用 `AGENTS.md` / MASTERPLAN 约定）：单关注提交、测试先行、`go test -race`/`vet`/`build` 全绿、不跨产品线。

---

## 7. 明确不做（锁定的非目标 · 防止再次漂移）

- ❌ 自建搜索索引（Exa/Brave 资本战）。
- ❌ 烧推理算力做 deep-research 循环（大玩家战场）。
- ❌ 正面做「通用可信搜索」硬刚 Parallel + Google；只做它们结构上做不了的 EAV + N_eff 两个洞。
- ❌ 自主浏览 agent（Firecrawl /agent 军备赛）。
- ❌ 区块链存证/事实期货/去中心化清算（SHA-256 + 可回放快照足够）。
- ❌ 与 LCI/lithium/DataOS/assimilation/安全产品线共享提交、目录或运行时（见 `SEARCH.md` 边界）。

---

## 8. 诚实的风险登记（一次说清，别自欺）

| 层 | 几乎确定成立 | 还没证明 / 是赌注 |
|---|---|---|
| **L1** | 数据平面快 100×、省 token、内容更多、结构上不幻觉——架构决定 | 自动发现在开放网络的鲁棒性；ToS 灰度 |
| **L2** | 便宜的重排器 > 烧推理算力（SOTA 已证） | 基础重排是大路货，**不融合 L3 就无护城河**；候选覆盖有上限 |
| **L3** | 你的证据/快照/收据已比 Parallel Basis 深 | EAV 从医疗窄域推广到开放网络是硬研究问题；整层的**市场需求未被大规模证明** |

**整体底线：** 别赌「三个单项都最强」（会输——重排拼不过开源、蛮力拼不过资本）。赌「**唯一一条从抓取到验证、信任不断线的栈**」。L1/L2 做到「够好」上牌桌，L3 的 EAV+N_eff 是让你**不可复制**的那根钉子。**这才是那股执着该用的地方——不是把每层磨到最亮，是把三层焊到别人拆不开。**

---

## 9. 一页速查（迷路时回这里）

```text
问题 ─────────────────────────────────────────► 可信的答案
                                                        ▲
  L1 抓取          L2 检索/重排         L3 验证           │
  把页面搞到手      找该读哪些、排序      能不能信、是不是那个东西
  ─────────       ─────────────       ────────────────
  已建: engine/    已建: search/       已建: evidence/ snapshot/
        cleaner/         (SEARCH.md)         receipts/ verify/
        compiler/                            consensus/(N_eff v0)
  新增: 数据平面    新增: 0.6B 重排器    新增: ① EAV 实体归因
        (值重叠归因)     + 信任信号排序        ② N_eff v1(出处血缘)

护城河 = 三层焊接：保住出处 → 按独立性+实体匹配排 → 抓 right-source-wrong-entity
不做   = 自建索引 / 烧推理算力 / 硬刚 Parallel / 浏览 agent / 区块链
顺序   = ①/search MVP → ②EAV → ③N_eff v1 → ④重排+信任排序 → ⑤数据平面
```

---

*本文档锁定三层结构与非目标。任务卡细节见 `MASTERPLAN.md`（判断层已建部分）、`SCRAPE-PLAN.md`（L1 质量门控）、`SEARCH.md`（L2 契约）。修订本文档需要一个明确的理由，并记录在此处的变更日志。*

### 变更日志
- 2026-08-10 v1.0：初次锁定。三层信任栈 + 三个新赌注（数据平面 / 重排+信任排序 / EAV+N_eff v1）+ 建造顺序 + 非目标。
- 2026-08-10 v1.0.1：§5.3 EAV 落点定案为新包 `verify/eav/`（不做 evidence/ 扩展），任务卡拆到 `MASTERPLAN.md` §15（E-1…E-6）。框架内细化，三层结构与非目标无变化。
